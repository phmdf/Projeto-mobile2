<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Oficina1">

        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.Oficina1">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".LoginActivity" />
        <activity android:name=".DashboardActivity" />
        <activity android:name=".PerfilActivity" />
        <activity android:name=".ClientesActivity" />
        <activity android:name=".VeiculosActivity" />
        <activity android:name=".HistoricoActivity" />
        <activity android:name=".OrcamentoActivity" />
        <activity android:name=".AtendimentoActivity" />
        <activity android:name=".NotificacoesActivity" />
        <activity android:name=".AprovacaoOrcamentoActivity" />
        <activity android:name=".CadastroVeiculoActivity" />
        <activity android:name=".CadastroUsuarioActivity" />

        <activity
            android:name=".MainActivity"
            android:exported="false" />

    </application>

</manifest>